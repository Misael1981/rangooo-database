{
  "name": "@misael1981/rangooo-database",
  "version": "1.2.4",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "generated-client",
    "prisma"
  ],
  "scripts": {
    "build": "npx prisma generate && tsc",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "push": "prisma db push",
    "studio": "prisma studio",
    "prepare": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "prisma": "^6.19.3",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
